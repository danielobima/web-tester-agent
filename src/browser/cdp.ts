export function normalizeCdpWsUrl(wsUrl: string, base: string): string {
  return wsUrl;
}

export type RawAXNode = any;
export type AriaSnapshotNode = any;

export function formatAriaSnapshot(nodes: any[], limit: number): any {
  return nodes;
}
